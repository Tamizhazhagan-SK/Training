[project]
name = "bmw-connected-car-sensor-api"
version = "1.0.0"
description = "API for ingesting BMW connected-vehicle telemetry and deriving health/alerts."
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "BMW Trainee" }
]
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.7.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["controllers", "services", "repositories", "models", "dtos"]
